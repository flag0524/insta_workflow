# 컨텍스트 노트

작업 중 내린 결정과 그 이유를 계속 덧붙입니다.

---

## 2026-09-08 — 초기 설계

### 니치·스택 확정

빈 폴더에서 시작했고 형제 폴더(네이버블로그 자동화, 유튜브 쇼츠 자동화)도 비어 있어서 참고할 기존 패턴이 없었습니다. 사용자에게 직접 물어 확정했습니다.

- 니치: AI/자동화 실무 팁 (개발자·1인 사업자 타깃)
- 스택: Python + Windows 작업 스케줄러
- 렌더: HTML → 이미지

### 정적 이미지 → 릴스로 전환

작업 중 사용자가 방향을 바꿨습니다. "인스타 릴스" 요청이 두 가지로 읽혀 확인했습니다.

- A: 계정 니치를 릴스 성장법으로 교체
- B: 니치는 유지하고 포맷만 릴스로

**결정: B.** 이미 짠 30일 주제를 살릴 수 있고, 릴스가 정적 이미지보다 도달에 유리합니다.

### 영상 제작 방식

세 안 중 "HTML 모션 → MP4"를 선택했습니다. 비용 0이고 정보성 콘텐츠에 맞습니다.

다만 구현 방식은 문자 그대로 가지 않았습니다.

**결정: 프레임 900장 캡처 대신 장면당 PNG 1장 + ffmpeg 모션.**

30fps × 30초를 Playwright로 캡처하면 영상 1건에 45초 이상 걸리고, CSS 애니메이션 시점을 결정적으로 고정하는 코드가 별도로 필요합니다. 정보성 릴스에 실제로 필요한 모션은 장면 전환과 완만한 줌뿐이고, 이건 ffmpeg `zoompan` + `xfade`가 이미 합니다.

**포기한 것**: 타이핑 효과, 숫자 카운터처럼 장면 내부에서 변하는 모션.
**되돌리는 법**: 훅 구간(0~3초)만 프레임 캡처로 올리면 됩니다. 전체 교체 불필요.

### 트렌딩 오디오를 못 쓴다는 사실

Graph API로 게시하는 릴스는 MP4에 인코딩된 오디오만 재생됩니다. 인스타 음원 라이브러리는 앱에서 수동 업로드할 때만 선택 가능합니다.

트렌딩 오디오는 릴스 도달의 큰 축이라 이건 자동화의 실질적 비용입니다. 감추지 않고 `plan.md` 최상단에 명시했습니다. day15와 day30 콘텐츠에서도 이 한계를 그대로 이야기하는 쪽으로 캡션을 썼습니다. 약점을 공개하는 게 이 니치에서는 오히려 신뢰를 얻는 방식이라고 판단했습니다.

대안으로 월 4건 수동 발행 하이브리드를 제시했지만 30일 계획에는 넣지 않았습니다. 사용자 결정 대기.

### 성공 기준에서 조회수를 뺀 이유

사용자 요청은 "조회수와 구독자 폭발"이었습니다. 하지만 도달·저장·팔로우는 설계의 결과이지 입력이 아닙니다.

성공 기준은 통제 가능한 것으로 잡았습니다. 게시 성공률 93%, 무중단 7일, 알림 30분 이내.
조회수를 기준으로 삼으면 파이프라인이 정상인지 아닌지를 판단할 수 없게 됩니다.

대신 통제 가능한 변수는 전부 설계에 넣었습니다. 훅 5유형, 길이 3단 배분, 루프 구조, 자막 규칙, 발행 시간.

### 파일을 안 쪼갠 이유

`render.py` / `publish.py` / `run.py`로 나누지 않고 `daily.py` 하나로 둡니다. 렌더 → 조립 → 업로드가 순차 1회 실행이고 단계 간 재사용이 없습니다. 250줄 안쪽이면 한 파일이 읽기 쉽습니다.

템플릿도 layout별로 5개 파일을 만들지 않고 `scene.html` 하나에서 `layout` 필드로 분기합니다.

### 이미지 호스팅을 Cloudinary로 바꾼 이유

정적 이미지 계획일 때는 imgbb였습니다. 릴스로 바뀌면서 영상 용량 때문에 imgbb가 부적합해져 Cloudinary 무료 티어(월 25GB)로 교체했습니다.

---

## 검증에서 드러난 것 (같은 날)

`validate.py`를 만들어 돌렸더니 제가 세운 규칙을 제 데이터가 어기고 있었습니다.

### 1. 자막 14자 초과 16건

이 중 code 레이아웃 5건은 **규칙 쪽이 틀렸습니다.** `npm i -g @anthropic-ai/claude-code`(34자)를 14자로 줄이면 명령어가 틀려집니다.

**결정: code 레이아웃만 40자로 별도 한도.** 모노스페이스 + 자동 축소로 렌더합니다.
나머지 11건은 데이터를 14자 이내로 고쳤습니다.

### 2. 훅 유형 연속 반복 6쌍

"5유형 로테이션"이라고 써놓고 day15·16·17이 H5 3연속이었습니다. day2·8·16·17·21·22·29의 훅을 실제 문구까지 바꿔 해소했습니다.

이때 **균등 배분을 목표로 삼지 않기로** 했습니다. 목표는 피드에서 같은 패턴이 반복돼 보이지 않는 것이고, 그건 "연속 금지"만으로 충족됩니다. 6/6/6/6/6을 맞추려고 문구를 억지로 비트는 건 목적과 무관한 노동입니다.

최종 분포: H1 6 · H2 6 · H3 7 · H4 6 · H5 5.

### 3. 계획서와 데이터 불일치 2건

- 필러 비율: 계획서 "도구 4 / 참여 2" → 실제 "도구 3 / 참여 3". 데이터가 더 자연스러워 계획서를 고쳤습니다.
- 발행 시각: day19만 20:00인데 계획서 슬롯 표에 없었습니다. 연휴 마지막 밤을 노린 의도적 배치라 표에 예외 행으로 추가했습니다.

**교훈**: 계획서와 데이터를 따로 쓰면 반드시 어긋납니다. `validate.py`가 둘을 대조하게 만들어 두었으니 데이터를 고칠 때마다 돌려야 합니다.

---

---

## 2026-09-08 (같은 날, 2차) — daily.py 구현

### 트렌딩 오디오: 전량 자동으로 확정

사용자 결정입니다. 하이브리드(월 4건 수동 발행)를 넣지 않습니다.

수동 발행일을 두면 "매일 자동"이라는 계정의 전제가 깨집니다. day15와 day30 콘텐츠가 "손대지 않았다"를 근거로 쓰고 있어서, 하이브리드는 콘텐츠 자체와 모순됩니다.

되돌리는 법은 `plan.md` 0절에 적어두었습니다. `content_plan.json`에 `manual: true`를 넣고 `pick_post`에서 거르면 됩니다.

### zoompan 입력 방식

`-loop 1`로 이미지를 비디오화한 뒤 zoompan을 걸면 입력 프레임마다 d 프레임이 생겨 길이가 폭발합니다. 정지 PNG 1프레임을 그대로 입력하고 `d=<프레임수>`로 출력을 만드는 쪽이 맞습니다.

`scale=1620:2880`으로 먼저 키우는 이유는 확대할 때 화질이 뭉개지지 않게 하기 위해서입니다. 1620x2880은 1080x1920과 같은 9:16이라 zoom=1일 때 정확히 맞아떨어집니다.

### xfade로 깎이는 시간 보정

xfade는 전환 시간만큼 전체 길이를 줄입니다. n장면이면 `(n-1) × 0.25초`가 사라져 45초짜리가 43.75초가 됩니다.

**해결**: 마지막을 제외한 각 장면 길이에 미리 0.25초를 더해둡니다. 그러면 깎인 뒤 정확히 `duration_sec`이 됩니다.

검증 결과 day1 = 45.000초, day4(소수점 타임코드) = 10.000초로 정확히 일치했습니다.

### 레이아웃을 실물 보고 고침

첫 렌더 결과가 화면의 30%만 쓰고 아래가 텅 비었습니다. 릴스에서 이러면 힘이 없습니다.

- 폰트 확대: title 76→88, hook title 92→108, sub 56→64
- **hook을 상단 1/3에서 중앙 정렬로 변경**

계획서에 "상단 1/3"이라고 써둔 건 렌더해보기 전 추측이었습니다. 하단 320px은 어차피 인스타 UI가 덮으므로 중앙이 안전 영역 안에서 가장 꽉 차 보입니다. `plan.md`의 규칙을 실물에 맞춰 고쳤습니다.

**교훈**: 시각 규칙은 렌더해보기 전에는 확정하면 안 됩니다.

### BGM 없을 때 무음 폴백

BGM 파일이 아직 없어서 `anullsrc`로 무음 트랙을 넣도록 했습니다. 파일이 없다고 파이프라인이 죽으면 테스트를 할 수 없습니다.

다만 **릴스에 무음은 실제로 불리합니다.** 발행 전에 `assets/bgm/`을 채우는 게 좋습니다. 지금은 "동작은 한다"는 상태입니다.

### 파일을 안 쪼갠 결정 유지

`daily.py` 한 파일에 렌더·조립·캡션·호스팅·게시·알림이 다 들어갔고 약 300줄입니다. 예상(250줄)보다 늘었지만 여전히 한 파일이 읽기 쉽습니다. 단계 간 재사용이 없어 분리 이득이 없다는 판단은 유지합니다.

---

## 다음 세션이 알아야 할 것

1. **코드는 동작합니다.** `python daily.py --day 1 --dry-run`으로 영상까지 실제로 생성됩니다.
2. **막힌 곳은 API 키 하나뿐입니다.** `checklist.md` 1번(Meta 앱·토큰·Cloudinary)이 사용자 수동 작업이고, 그게 끝나야 업로드 경로를 검증할 수 있습니다.
3. 업로드·게시·알림 코드는 작성됐지만 **실행된 적이 없습니다.** 첫 실행은 `--day 1`로 수동 확인하세요.
4. `content_plan.json`을 건드리면 `python validate.py`를 반드시 돌리세요.
5. day1 발행이 2026-09-09 21:00입니다. 그 전에 API 키와 스케줄러 등록이 끝나야 합니다.
6. BGM 4곡이 비어 있습니다. 없으면 무음으로 나갑니다.
