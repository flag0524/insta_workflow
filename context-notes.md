# 컨텍스트 노트

작업 중 내린 결정과 그 이유를 계속 덧붙입니다.

---

## 2026-09-08 — 초기 설계

### 니치·스택 확정

빈 폴더에서 시작했고 형제 폴더(네이버블로그 자동화, 유튜브 쇼츠 자동화)도 비어 있어서 참고할 기존 패턴이 없었습니다. 사용자에게 직접 물어 확정했습니다.

- 니치: AI/자동화 실무 팁 (개발자·1인 사업자 타깃)
- 스택: Python + Windows 작업 스케줄러
- 렌더: HTML → 이미지

### 정적 이미지 → 릴스로 전환

작업 중 사용자가 방향을 바꿨습니다. "인스타 릴스" 요청이 두 가지로 읽혀 확인했습니다.

- A: 계정 니치를 릴스 성장법으로 교체
- B: 니치는 유지하고 포맷만 릴스로

**결정: B.** 이미 짠 30일 주제를 살릴 수 있고, 릴스가 정적 이미지보다 도달에 유리합니다.

### 영상 제작 방식

세 안 중 "HTML 모션 → MP4"를 선택했습니다. 비용 0이고 정보성 콘텐츠에 맞습니다.

다만 구현 방식은 문자 그대로 가지 않았습니다.

**결정: 프레임 900장 캡처 대신 장면당 PNG 1장 + ffmpeg 모션.**

30fps × 30초를 Playwright로 캡처하면 영상 1건에 45초 이상 걸리고, CSS 애니메이션 시점을 결정적으로 고정하는 코드가 별도로 필요합니다. 정보성 릴스에 실제로 필요한 모션은 장면 전환과 완만한 줌뿐이고, 이건 ffmpeg `zoompan` + `xfade`가 이미 합니다.

**포기한 것**: 타이핑 효과, 숫자 카운터처럼 장면 내부에서 변하는 모션.
**되돌리는 법**: 훅 구간(0~3초)만 프레임 캡처로 올리면 됩니다. 전체 교체 불필요.

### 트렌딩 오디오를 못 쓴다는 사실

Graph API로 게시하는 릴스는 MP4에 인코딩된 오디오만 재생됩니다. 인스타 음원 라이브러리는 앱에서 수동 업로드할 때만 선택 가능합니다.

트렌딩 오디오는 릴스 도달의 큰 축이라 이건 자동화의 실질적 비용입니다. 감추지 않고 `plan.md` 최상단에 명시했습니다. day15와 day30 콘텐츠에서도 이 한계를 그대로 이야기하는 쪽으로 캡션을 썼습니다. 약점을 공개하는 게 이 니치에서는 오히려 신뢰를 얻는 방식이라고 판단했습니다.

대안으로 월 4건 수동 발행 하이브리드를 제시했지만 30일 계획에는 넣지 않았습니다. 사용자 결정 대기.

### 성공 기준에서 조회수를 뺀 이유

사용자 요청은 "조회수와 구독자 폭발"이었습니다. 하지만 도달·저장·팔로우는 설계의 결과이지 입력이 아닙니다.

성공 기준은 통제 가능한 것으로 잡았습니다. 게시 성공률 93%, 무중단 7일, 알림 30분 이내.
조회수를 기준으로 삼으면 파이프라인이 정상인지 아닌지를 판단할 수 없게 됩니다.

대신 통제 가능한 변수는 전부 설계에 넣었습니다. 훅 5유형, 길이 3단 배분, 루프 구조, 자막 규칙, 발행 시간.

### 파일을 안 쪼갠 이유

`render.py` / `publish.py` / `run.py`로 나누지 않고 `daily.py` 하나로 둡니다. 렌더 → 조립 → 업로드가 순차 1회 실행이고 단계 간 재사용이 없습니다. 250줄 안쪽이면 한 파일이 읽기 쉽습니다.

템플릿도 layout별로 5개 파일을 만들지 않고 `scene.html` 하나에서 `layout` 필드로 분기합니다.

### 이미지 호스팅을 Cloudinary로 바꾼 이유

정적 이미지 계획일 때는 imgbb였습니다. 릴스로 바뀌면서 영상 용량 때문에 imgbb가 부적합해져 Cloudinary 무료 티어(월 25GB)로 교체했습니다.

---

## 검증에서 드러난 것 (같은 날)

`validate.py`를 만들어 돌렸더니 제가 세운 규칙을 제 데이터가 어기고 있었습니다.

### 1. 자막 14자 초과 16건

이 중 code 레이아웃 5건은 **규칙 쪽이 틀렸습니다.** `npm i -g @anthropic-ai/claude-code`(34자)를 14자로 줄이면 명령어가 틀려집니다.

**결정: code 레이아웃만 40자로 별도 한도.** 모노스페이스 + 자동 축소로 렌더합니다.
나머지 11건은 데이터를 14자 이내로 고쳤습니다.

### 2. 훅 유형 연속 반복 6쌍

"5유형 로테이션"이라고 써놓고 day15·16·17이 H5 3연속이었습니다. day2·8·16·17·21·22·29의 훅을 실제 문구까지 바꿔 해소했습니다.

이때 **균등 배분을 목표로 삼지 않기로** 했습니다. 목표는 피드에서 같은 패턴이 반복돼 보이지 않는 것이고, 그건 "연속 금지"만으로 충족됩니다. 6/6/6/6/6을 맞추려고 문구를 억지로 비트는 건 목적과 무관한 노동입니다.

최종 분포: H1 6 · H2 6 · H3 7 · H4 6 · H5 5.

### 3. 계획서와 데이터 불일치 2건

- 필러 비율: 계획서 "도구 4 / 참여 2" → 실제 "도구 3 / 참여 3". 데이터가 더 자연스러워 계획서를 고쳤습니다.
- 발행 시각: day19만 20:00인데 계획서 슬롯 표에 없었습니다. 연휴 마지막 밤을 노린 의도적 배치라 표에 예외 행으로 추가했습니다.

**교훈**: 계획서와 데이터를 따로 쓰면 반드시 어긋납니다. `validate.py`가 둘을 대조하게 만들어 두었으니 데이터를 고칠 때마다 돌려야 합니다.

---

---

## 2026-09-08 (같은 날, 2차) — daily.py 구현

### 트렌딩 오디오: 전량 자동으로 확정

사용자 결정입니다. 하이브리드(월 4건 수동 발행)를 넣지 않습니다.

수동 발행일을 두면 "매일 자동"이라는 계정의 전제가 깨집니다. day15와 day30 콘텐츠가 "손대지 않았다"를 근거로 쓰고 있어서, 하이브리드는 콘텐츠 자체와 모순됩니다.

되돌리는 법은 `plan.md` 0절에 적어두었습니다. `content_plan.json`에 `manual: true`를 넣고 `pick_post`에서 거르면 됩니다.

### zoompan 입력 방식

`-loop 1`로 이미지를 비디오화한 뒤 zoompan을 걸면 입력 프레임마다 d 프레임이 생겨 길이가 폭발합니다. 정지 PNG 1프레임을 그대로 입력하고 `d=<프레임수>`로 출력을 만드는 쪽이 맞습니다.

`scale=1620:2880`으로 먼저 키우는 이유는 확대할 때 화질이 뭉개지지 않게 하기 위해서입니다. 1620x2880은 1080x1920과 같은 9:16이라 zoom=1일 때 정확히 맞아떨어집니다.

### xfade로 깎이는 시간 보정

xfade는 전환 시간만큼 전체 길이를 줄입니다. n장면이면 `(n-1) × 0.25초`가 사라져 45초짜리가 43.75초가 됩니다.

**해결**: 마지막을 제외한 각 장면 길이에 미리 0.25초를 더해둡니다. 그러면 깎인 뒤 정확히 `duration_sec`이 됩니다.

검증 결과 day1 = 45.000초, day4(소수점 타임코드) = 10.000초로 정확히 일치했습니다.

### 레이아웃을 실물 보고 고침

첫 렌더 결과가 화면의 30%만 쓰고 아래가 텅 비었습니다. 릴스에서 이러면 힘이 없습니다.

- 폰트 확대: title 76→88, hook title 92→108, sub 56→64
- **hook을 상단 1/3에서 중앙 정렬로 변경**

계획서에 "상단 1/3"이라고 써둔 건 렌더해보기 전 추측이었습니다. 하단 320px은 어차피 인스타 UI가 덮으므로 중앙이 안전 영역 안에서 가장 꽉 차 보입니다. `plan.md`의 규칙을 실물에 맞춰 고쳤습니다.

**교훈**: 시각 규칙은 렌더해보기 전에는 확정하면 안 됩니다.

### BGM 없을 때 무음 폴백

BGM 파일이 아직 없어서 `anullsrc`로 무음 트랙을 넣도록 했습니다. 파일이 없다고 파이프라인이 죽으면 테스트를 할 수 없습니다.

다만 **릴스에 무음은 실제로 불리합니다.** 발행 전에 `assets/bgm/`을 채우는 게 좋습니다. 지금은 "동작은 한다"는 상태입니다.

### 파일을 안 쪼갠 결정 유지

`daily.py` 한 파일에 렌더·조립·캡션·호스팅·게시·알림이 다 들어갔고 약 300줄입니다. 예상(250줄)보다 늘었지만 여전히 한 파일이 읽기 쉽습니다. 단계 간 재사용이 없어 분리 이득이 없다는 판단은 유지합니다.

---

## 2026-09-08 (3차) — 전량 검증과 환경 함정

### 30일 전량 dry-run: 30/30 성공

3일치만 테스트하고 넘어가려다 전체를 돌렸습니다. 실패 0건이고 모든 영상 길이가 계획값과 정확히 일치했습니다.

레이아웃 5종, 장면 수 3~7개, 소수점 타임코드, 8~55초 길이 범위가 전부 커버됐습니다. 배포 전 전량 검증은 값이 있었습니다.

### python이 두 개다 — 스케줄러의 함정

**이게 이번에 제일 위험했던 부분입니다.**

PATH의 `python`은 `AppData\Local\hermes\hermes-agent\venv\Scripts\python.exe`를 가리킵니다. 에이전트 도구의 가상환경입니다. `pip install playwright cloudinary`가 전부 여기로 들어갔습니다.

사용자 시스템 python은 `AppData\Local\Programs\Python\Python311\python.exe`이고, 여기엔 playwright와 cloudinary가 **없었습니다.**

그대로 작업 스케줄러를 등록했다면 매일 ImportError로 조용히 실패했을 겁니다. 텔레그램 알림도 아직 없으니 며칠 뒤에나 알아챘을 상황입니다.

**조치**: 시스템 python에 패키지를 따로 설치하고, 스케줄러에는 venv가 아니라 **시스템 python의 절대 경로**를 등록합니다.

**교훈**: 개발 중 검증에 쓴 인터프리터와 운영이 실제로 쓸 인터프리터가 다를 수 있습니다. 스케줄러·크론 등록 전에는 "그 실행 주체가 이 패키지를 볼 수 있는가"를 반드시 확인해야 합니다.

### 저장소 이름

`instar_workflow`가 아니라 `insta_workflow`였습니다. 오타 확인을 요청해두길 잘했습니다. remote를 고쳐 push했습니다.

---

### 스케줄러 등록 완료

`인스타릴스-아침`(07:15) / `-오전`(09:45) / `-저녁`(20:15) 3개를 등록했습니다. StartWhenAvailable을 켜서 PC가 꺼져 있던 시간대의 작업도 켜자마자 처리됩니다.

수동 실행으로 검증했습니다. `LastTaskResult=0`이고 `daily.log`에 "오늘 예정된 게시물 없음"이 기록됐습니다. 오늘(09-08)은 계획에 없는 날이라 이게 정상 동작입니다. 스케줄러 → 시스템 python → daily.py → 로그까지 전 경로가 확인됐습니다.

### 파일 로그 추가

스케줄러로 실행하면 표준출력이 어디에도 남지 않습니다. `log()`가 `daily.log`에도 쓰도록 3줄 고쳤습니다.

day29 콘텐츠가 "로그 남기기는 자동화의 생명줄"인데 정작 우리 파이프라인에 로그 파일이 없었습니다. 텔레그램 토큰이 아직 없으니 당분간은 이 파일이 실패를 알 수 있는 유일한 단서입니다.

---

## 2026-09-08 (4차) — 실전 게시 성공

### 전 구간 실측

day1이 실제로 게시됐습니다. https://www.instagram.com/reel/DdBRh_mk8ey/

렌더 3초 → 조립 33초 → Cloudinary 5초 → 컨테이너 생성 2초 → **인코딩 대기 72초** → 게시.
전체 2분. 인코딩이 가장 오래 걸리므로 폴링 상한 5분은 적절합니다.

`media_product_type`이 `REELS`로 확인됐습니다. 피드 영상으로 잘못 올라가지 않았습니다.

### 토큰이 Instagram Login 계열이었다

제 초기 설계는 `graph.facebook.com`(Facebook Login 방식)을 전제했고 "페이스북 페이지 연결 필수"라고 적었습니다. 실제 계정 토큰은 `graph.instagram.com`(Instagram Login 방식)이었고, 이 방식은 **페이지 연결이 필요 없습니다.**

`GRAPH` 상수 한 줄만 바꾸니 나머지 흐름은 그대로 동작했습니다. 두 계열의 게시 엔드포인트 구조가 같기 때문입니다.

**교훈**: 외부 API는 "어느 계열의 토큰인가"부터 확인해야 합니다. 문서를 읽고 세운 전제가 실제 자격증명과 다를 수 있습니다.

### check-env가 두 번 느슨했다

값의 존재만 검사해서 두 번 놓쳤습니다.

1. `IG_USER_ID`에 사용자명(`flag_21`)이 들어 있었는데 통과 — 숫자 ID여야 합니다
2. `CLOUDINARY_API_SECRET`에 별표 10개(`**********`)가 들어 있었는데 통과 — 대시보드의 가려진 표시를 복사한 값

둘 다 실제 API 호출을 추가해 잡도록 고쳤습니다. **자격증명 검사는 존재가 아니라 인증으로 해야 합니다.**

### BGM은 합성음이다

실제 음원 대신 ffmpeg 사인파 합성으로 만들었습니다(`make_bgm.py`). 저음 드론 화음 + 주기적으로 감쇠하는 펄스 구조입니다.

솔직히 음악이라기엔 부족합니다. 무음보다 낫다는 수준이고, 실제 음원을 구하면 같은 파일명으로 덮어쓰면 됩니다. mp3는 gitignore 대상이라 다른 환경에서는 `make_bgm.py`를 돌려 재생성합니다.

---

## 2026-09-08 (5차) — 일정 하루 당김

day1을 예정일(09-09)보다 하루 일찍 게시해서, 전체 일정을 09-08 ~ 10-07로 당겼습니다.

### 단순 -1일이 안 되는 이유

날짜를 하루 당기면 **요일이 하루씩 밀립니다.** 그러면 두 가지가 깨집니다.

1. 주말용 8~12초 루프 콘텐츠가 평일에 배치됩니다
2. **"추석입니다" 콘텐츠가 추석 하루 전에 나갑니다**

콘텐츠 내용이 날짜에 묶여 있어서 날짜 필드만 고치면 안 됩니다.

### 재배치 방식

날짜를 옮긴 뒤 콘텐츠를 새 요일에 맞게 다시 배치했습니다.

- 새 일정의 주말·연휴 슬롯이 정확히 10개, 루프형 콘텐츠도 10건 → 일대일 대응
- 원래 순서를 유지한 채 배치하니 연휴 정합성이 **오히려 개선**됐습니다

| day | 날짜 | 콘텐츠 |
|---|---|---|
| 17 | 09-24(목) 연휴 첫날 | 연휴에도 계정이 도는 이유 |
| 18 | 09-25(금) 추석 당일 | 추석 인사 |
| 20 | 09-27(일) 연휴 마지막 20:00 | 연휴 끝 체크리스트 |

이전 계획에서는 추석 인사가 9/25가 아니라 하루 어긋나 있었는데, 이번 재배치로 정확히 맞았습니다.

발행 시각도 새 요일 규칙으로 재계산했습니다 (화·목 08:00 / 월·수·금 21:00 / 주말·연휴 10:30).

**교훈**: 콘텐츠가 특정 날짜(명절, 요일)를 전제하면 일정 변경은 데이터 이동이 아니라 재배치 문제입니다.

참고로 day1의 `publish_at`은 규칙대로 08:00으로 적혀 있지만 실제 게시는 17:38이었습니다. 이미 `state.json`에 기록돼 다시 올라가지 않으므로 무해합니다.

---

## 2026-09-12 — 운영 4일 만에 드러난 설계 결함

첫 주 실적: day1·3·4 게시, **day2 누락**, day5 예정.

### 무슨 일이 있었나

- 09-09 20:52 day2 → Cloudinary 업로드까지 성공했으나 인스타 인코딩이 `ERROR` 반환. 실패 처리 후 종료
- 09-10 이후 **아무도 day2를 다시 시도하지 않음**
- 09-11 day4 → 20:15 트리거 시각에 PC가 꺼져 있었고, 09-12 01:41에야 밀린 작업이 실행됨. 그때는 이미 날짜가 바뀌어 day5를 찾고 "발행 시각 전"으로 종료

### 근본 원인

`pick_post`가 **오늘 날짜 항목만** 조회했습니다. 계획서에는 "실패 시 다음 트리거에서 재시도"라고 적어뒀지만, 그 재시도는 **같은 날 안에서만** 유효했습니다. 자정을 넘기면 실패한 게시물은 영구 누락됩니다.

하루 3회 트리거를 두고 `StartWhenAvailable`까지 켜서 안전하다고 생각했는데, 둘 다 "같은 날"이라는 전제 위에 있었습니다.

### 수정

1. 오늘 항목이 없거나 이미 게시됐으면 **지난 미게시 항목을 오래된 순으로 따라잡습니다** (`CATCHUP_DAYS`=3)
2. `date_sensitive: true`인 항목(day17 연휴 첫날, day18 추석, day20 연휴 마지막)은 그날이 지나면 건너뜁니다. "추석입니다"가 이틀 뒤에 나가면 안 되니까요

인코딩 ERROR 자체의 즉시 재시도는 넣지 않았습니다. 따라잡기가 있으면 다음 트리거가 알아서 다시 만들어 올립니다.

### 테스트를 안 돌린 대가

09-08 일정 재조정 커밋에서 `test_pick_post.py`를 돌리지 않았습니다. 날짜를 하루 당겼으니 날짜를 하드코딩한 테스트가 그 시점에 이미 깨져 있었는데, 오늘에서야 발견했습니다.

테스트가 살아 있었다면 재조정 직후 빨간불이 떴을 겁니다. **데이터를 고치면 그 데이터를 전제한 테스트도 함께 돌려야 합니다.**

테스트를 새 날짜 기준으로 다시 쓰고, day2 누락 상황을 재현하는 케이스를 넣었습니다(13개).

---

## 2026-09-16 — 카드뉴스 라이트 테마 추가, 인코딩 불안정 재확인

### 카드뉴스 테마 (완료, 검증됨)

사용자가 첨부한 참고 이미지(흰 카드, 남색 번호 배지, 민트 아이콘 원)의 톤을 새 템플릿으로 추가했습니다.

- `templates/card.html` — 밝은 민트/하늘색 톤. `scene.html`(다크)과 같은 5개 layout 분기, 번호 배지(`num`)와 인라인 SVG 아이콘(`icon`) 추가
- `daily.py`에 `TEMPLATES` dict + `ICON_SVG` dict 추가. `post.get("theme", "dark")`로 테마 선택 — 기존 29개 게시물은 무영향
- 아이콘은 외부 라이브러리 없이 인라인 SVG로 4종 직접 제작 (file/checklist/link/bookmark)
- day7에 `theme: "card"` 적용, 렌더 결과를 이미지로 확인 후 사용자 승인 받고 진행

`--dry-run` 렌더와 로컬 재생 확인까지는 전부 정상. 문제는 그다음 실제 게시 단계에서 나왔습니다.

### 인코딩 ERROR: 제 첫 번째 진단이 틀렸습니다

09-13 세션에서 "재시도만 하면 대부분 해결된다"고 결론 내리고 `ENCODE_RETRIES`를 넣었는데, 오늘 그 결론이 반박됐습니다.

1. day7 자동 재시도 2회 모두 실패 (5~6초 만에)
2. 대기 간격을 넓혀도(10초) 3회 모두 실패
3. **대조군 테스트**: 이미 8일 전 성공적으로 게시됐던 day1 영상을 지금 다시 올려도 5초 만에 ERROR

3번이 결정적입니다. day7이나 카드 테마의 문제가 아니라 **오늘 인스타그램 쪽 인코딩이 전반적으로 불안정**하다는 뜻입니다. 새 코드는 무죄입니다 — 렌더링, 로컬 디코딩, Cloudinary 업로드 전부 정상 확인됐습니다.

**의심되는 요인**: 오늘 진단하느라 컨테이너를 15개 이상 만들었습니다. 실패까지 걸리는 시간이 처음엔 18~72초였다가 나중엔 5초로 급격히 짧아진 패턴이, 문서화되지 않은 처리량 제한에 걸렸을 가능성과 맞아떨어집니다. 확신할 수는 없지만, 더 두드릴수록 악화될 위험이 있어 라이브 테스트를 중단했습니다.

**교훈**: 재현 1~2건으로 "고쳤다"고 결론 내리면 안 됩니다. 대조군(이미 성공했던 것도 지금 실패하는가) 없이는 무엇을 고쳤는지 알 수 없습니다.

### 지금 상태

- day7은 `state.json`에 없음 (미게시, 중복 위험 없음)
- 카드뉴스 코드(`card.html`, `daily.py`의 테마/아이콘 지원)는 완성·커밋 대상
- `ENCODE_RETRIES=3` + `ENCODE_RETRY_WAIT=10`은 남겨둡니다. 도움이 안 된 것으로 보이지만 해도 되지 않을 이유도 없고, 다음에 진짜 일시적 오류일 때는 여전히 유효합니다
- 재시도는 다음 예약 트리거(따라잡기 로직)에 맡김. 지금 더 두드리지 않음

---

## 2026-09-17 — 동시 실행 레이스 컨디션 발견·수정

### 진짜 원인을 찾았습니다

09-16 밤 PC가 꺼져 있어 저녁(20:15)과 다음 날 아침(07:15) 트리거가 둘 다 밀렸습니다. 09-17 08:58에 PC가 켜지자 두 작업이 **완전히 같은 초(08:58:01)에** 동시 실행됐습니다. `daily.py` 프로세스 2개가 동시에 day10을 집어 같은 경로(`out/day10/`)에 동시에 썼고, 한쪽이 다른 쪽이 쓰는 도중인 파일을 Cloudinary에 업로드하면서 `All parts except EOF-chunk must be larger than 5mb` 에러가 났습니다.

Windows 작업 스케줄러의 "MultipleInstances: IgnoreNew"는 **같은 작업**의 중복 실행만 막습니다. 서로 다른 작업 3개(아침/오전/저녁)가 같은 `daily.py`를 부르는 건 스케줄러 설정으로 막을 수 없어서, 코드 쪽에 원자적 파일 락(`daily.lock`, `os.O_CREAT|O_EXCL`)을 추가했습니다.

- 25분 넘은 락은 죽은 프로세스로 보고 넘겨받음 (렌더+업로드+인코딩 실측 최대 3~4분이라 여유 있게 잡음)
- 검증: 동시 실행 2개 시뮬레이션(1개만 진행) / 오래된 락 넘겨받기 / 정상 종료 시 락 정리 / 기존 테스트 전부 통과

### 고쳐도 안 되는 게 남아있습니다

레이스 수정 후 재렌더한 깨끗한 day10 영상도 인스타 인코딩이 3번 다 ERROR였습니다. 계정·게시 한도는 정상. 09-16의 "계정 전체가 불안정"했던 패턴이 09-17에도 이어지고 있어, 순수 일시적 오류가 아니라 **이 계정에 걸린 제한**일 가능성을 배제할 수 없습니다. 계속된 재시도가 상황을 악화시킬 수 있어 라이브 테스트를 멈췄습니다. day10은 미게시로 안전하게 대기, 다음 트리거가 자동 재시도합니다.

이틀 연속 재현되면 Meta Business Suite에서 계정 제한 여부를 직접 확인하는 게 다음 단계입니다. 이건 코드로 해결할 수 있는 범위 밖입니다.

---

## 2026-09-17 (2차) — GitHub Actions로 이전

### 결정 이유

바로 이전에 잡은 동시 실행 레이스 컨디션(파일 락으로 수정)의 근본 원인은 결국 "로컬 PC 전원 상태에 실행이 묶여 있다"는 것이었습니다. PC가 꺼져 있으면 트리거가 밀리고, 밀린 트리거가 부팅 시점에 몰려서 겹칩니다. 파일 락은 증상을 막았지만, 애초에 로컬 스케줄러 자체가 이 클래스의 문제를 계속 만들어낼 구조였습니다.

사용자가 GitHub Actions 이전을 요청해서 옮겼습니다. GitHub 서버에서 정시 실행되므로 로컬 PC 전원과 무관하고, `concurrency` 설정으로 겹치는 실행을 아예 대기열로 직렬화할 수 있습니다.

### state.json을 git 추적으로 전환

가장 중요한 구조 변경입니다. 로컬 실행은 디스크에 상태가 남지만, GitHub Actions는 매번 새 체크아웃입니다. `state.json`을 `.gitignore`에서 빼고 리포에 커밋했고, 워크플로우 마지막 단계가 실행 후 변경분을 자동으로 커밋·푸시하도록 했습니다. 이걸 빼먹으면 매 실행마다 이력을 잃어버려 매번 첫날부터 다시 시도하거나, 최악의 경우 이미 올린 걸 중복 게시합니다.

daily.log는 반대로 git에 넣지 않기로 했습니다. GitHub Actions 자체의 실행 로그가 이미 영구 기록이라 굳이 파일로 한 번 더 커밋할 이유가 없습니다.

### BGM은 리포에 안 넣고 매번 재생성

`assets/bgm/*.mp3`는 여전히 gitignore 대상입니다. 라이선스가 있는 실제 파일이 아니라 `make_bgm.py`의 ffmpeg 사인파 합성이라, 바이너리를 리포에 쌓아두는 대신 워크플로우가 매번 `python make_bgm.py`로 재생성합니다. 결정적(deterministic)이라 매번 같은 결과가 나오고, 리포는 깨끗하게 유지됩니다.

### 로컬 스케줄러는 삭제 대신 비활성화

`Disable-ScheduledTask`로 껐습니다. 삭제와 달리 `Enable-ScheduledTask` 한 줄로 되돌릴 수 있습니다. 로컬과 GitHub Actions가 동시에 살아있으면 같은 클래스의 레이스가 두 시스템 사이에서 다시 날 수 있어 반드시 하나만 켜져 있어야 합니다.

### 이 세션이 끝내지 못한 것

`gh` CLI가 이 환경에 설치돼 있지 않고, 설치하더라도 `gh auth login`은 브라우저 인증이 필요해 비대화형 세션에서 완주할 수 없습니다. 그래서:

1. **GitHub Secrets 7개는 사용자가 직접 등록해야 합니다.** 로컬 `.env`에 이미 있는 값 그대로 리포지토리 Settings에 넣으면 됩니다.
2. **첫 실행도 사용자가 Actions 탭에서 `workflow_dispatch`로 수동 트리거해야 합니다.** 제가 대신 실행해볼 방법이 없습니다.

두 가지가 끝나야 이 마이그레이션이 실전 검증됩니다. `.github/workflows/daily-post.yml`은 YAML 문법 검증만 마친 상태입니다.

---

## 2026-09-17 (3차) — 스크립트 인젝션 수정 + 첫 실전 실행 확인

### 보안 검토에서 잡은 것

커밋 후 자동 보안 검토가 `daily-post.yml`의 스크립트 인젝션 취약점을 잡았습니다. `workflow_dispatch`의 `day`/`dry_run` 입력값을 `run:` 스크립트 텍스트에 `${{ }}`로 직접 끼워 넣고 있었는데, 이 치환은 셸이 스크립트를 파싱하기 **전에** 문자 그대로 이뤄져서, 입력값에 `;`나 백틱 같은 셸 메타문자가 있으면 같은 job의 시크릿(IG_ACCESS_TOKEN 등)에 접근하는 임의 명령 실행으로 이어질 수 있었습니다.

`workflow_dispatch`는 쓰기 권한자만 트리거 가능해 위험도는 제한적이지만, 표준대로 고쳤습니다. 입력값을 `env:`로 넘기고 스크립트에서는 환경변수(`$INPUT_DAY`)로만 참조합니다. 환경변수 값은 셸이 문법으로 재해석하지 않고 값 그대로 치환되므로 인젝션 경로가 막힙니다. `ARGS`도 문자열 concat 대신 배열로 바꿔 따옴표 처리를 정리했습니다.

### 사용자가 이미 첫 실행을 마쳤습니다

수정을 푸시하려는데 원격에 제가 모르는 커밋(`43f4a21 chore: 게시 이력 갱신`)이 있었습니다. **사용자가 이미 시크릿을 등록하고 워크플로우를 실행했고, day10이 실제로 게시됐습니다** (2026-09-17 11:02:06, media_id `18115764571961653`).

중요한 점: 로컬에서는 day10이 인코딩 ERROR로 계속 실패했는데(같은 날 아침), GitHub Actions에서는 성공했습니다. "이 계정에 제한이 걸렸을 수 있다"던 09-16~09-17 오전의 의심은 결과적으로 계정 문제가 아니었을 가능성이 높습니다 — 로컬 환경(그날 진단하며 만든 컨테이너 수, 또는 로컬 네트워크 경로)과 관련된 일시적 문제였을 수 있습니다. GitHub Actions 러너에서는 재현되지 않았습니다.

state.json은 addition-only라 충돌 없이 rebase로 병합했습니다.

---

## 2026-09-19 — "API access blocked" 차단 (진행 중, 사용자 확인 필요)

### 확인된 사실

- 마지막 성공 게시는 day9(09-18 00:35 KST). 이후 GitHub Actions 예약 실행 4회(09-18 09:12·14:09, 09-19 00:00·09:16)가 모두 `환경 점검`(토큰 검증) 단계에서 실패했습니다. 게시 단계까지 가지 못해 **중복 게시나 state.json 손상은 없습니다.**
- 응답은 HTTP 400 `{"error":{"message":"API access blocked.","type":"OAuthException","code":200}}`, 헤더는 `WWW-Authenticate: OAuth "Facebook Platform" "access_denied" "API access blocked."`
- **GitHub 러너와 로컬(서로 다른 IP)에서 똑같이 막힙니다.** 실행 환경 문제가 아닙니다.
- 응답 헤더 `x-app-usage: {"call_volume":0,"cpu_time":0}` — 요청 한도 초과가 아닙니다. 09-16에 세웠던 "진단하며 컨테이너를 많이 만들어 처리량 제한에 걸렸을 수 있다"는 가설은 이 증거로 기각합니다.
- 토큰 형식은 정상(183자, `IGAA` 접두)이고 만료·파싱 불가(code 190)와도 다른 응답입니다. 토큰은 **인식되지만 접근이 거부**됩니다(`access_denied`).

### 모르는 것

원인이 **앱 제한 / 계정 제한 / 토큰 무효화** 중 무엇인지는 API 응답만으로 구분할 수 없습니다. Meta 앱 대시보드와 인스타그램 계정 상태에서만 확인됩니다. 09-15~17의 인코딩 ERROR와 같은 뿌리인지도 알 수 없습니다. 같은 원인이라고 단정하지 마세요.

### 부수 관찰 (이번 조사에서 발견)

1. **예약 실행이 크론 시각보다 2~4.5시간 늦게 뜹니다.** 07:15 KST 크론은 09:12~09:16경, 09:45는 14:09~14:25경, 20:15는 다음 날 00:00~00:33경에 실행됐습니다. GitHub 예약 실행 지연입니다. 발행창은 "이 시각 이후"만 걸어서 게시 자체엔 지장이 없지만, 21:00 슬롯은 실제로 자정 이후에 나갑니다.
2. **차단이 길어지면 따라잡기 절벽이 옵니다.** `CATCHUP_DAYS=3`이라 day11(09-18)은 09-22부터, day12(09-19)는 09-23부터 자동으로 스킵됩니다. 한 번에 1건만 올리고 오늘 것이 우선이라 차단이 풀려도 밀린 걸 소화하는 속도가 느립니다.

---

## 2026-09-19 (2차) — 차단 자연 해제, 밀린 게시물 복구

13:52경 `--check-env`가 다시 통과했습니다(`토큰 검증: OK — @flag_21`). `.env`는 09-17 11:03 이후 수정된 적이 없어서 **토큰을 바꿔서 풀린 게 아니라 Meta 쪽 차단이 스스로 해제**된 것입니다. 09-18 00:35경부터 09-19 13:52경까지 약 37시간 지속됐고, 원인(앱/계정/토큰)은 끝내 확인하지 못했습니다.

로컬 예외 경로로 날짜순 복구했습니다. day11(예정 09-18)을 13:54에, day12(예정 09-19)를 13:55에 게시했고 각각 즉시 `state.json`을 push했습니다. 게시 전에 GitHub Actions에 진행 중인 실행이 없는지 공개 API로 확인했습니다. 예약 실행이 같은 시점에 떠서 같은 밀린 게시물을 집으면 중복 게시가 나기 때문입니다. 계정 게시물 수는 23개(기존 12 + 자동화 11)로 대조가 맞습니다.

교훈: 이번 차단은 "코드로 해결할 수 없는 외부 문제"라고 판단해 재시도를 멈춘 게 맞았습니다. 반복 호출로 밀어붙였다면 원인도 못 밝히고 다른 문제를 만들 수 있었습니다.

---

## 다음 세션이 알아야 할 것 (2026-09-19 최신)

1. **"API access blocked" 차단은 09-19 13:52경 자연 해제됐고 day1~12가 모두 복구됐습니다.** 원인은 끝내 불명입니다. 재발하면 코드보다 먼저 `x-app-usage`(한도 여부)와 `WWW-Authenticate` 헤더를 확인하고, 토큰을 재발급하면 **GitHub Secret `IG_ACCESS_TOKEN`과 로컬 `.env` 두 곳 모두** 갱신하세요. 차단 중에도 GitHub Actions는 `환경 점검`에서 즉시 멈춰 무해했습니다. **로컬 수동 게시는 게시 직전에 GitHub Actions 진행 중 실행이 없는지 확인하고, 게시 직후 바로 `state.json`을 push하세요**(예약 실행이 밀린 게시물을 집어 중복 게시될 수 있음).
2. **GitHub Actions 자체는 09-17~18에 실전 검증됐습니다.** day10·day9를 자동 게시했고 `state.json` 자동 커밋도 확인했습니다. `인스타릴스-*` 로컬 작업 3개는 비활성화 상태(삭제 아님)이며 더는 쓰지 않습니다. 인코딩 ERROR와 이번 차단이 같은 원인인지는 불명이니 하나로 단정하지 마세요.
3. **게시 이력은 `state.json`이 git에 커밋되는 것으로 관리합니다.** 워크플로우가 실행 후 자동으로 커밋·푸시합니다. 로컬에서 실제 게시(`--dry-run` 없이)는 하지 마세요 — 하려면 먼저 `git pull`, 끝나면 바로 `git push`.
4. **스크립트 인젝션 취약점을 수정했습니다.** `workflow_dispatch` 입력값은 `run:` 스크립트에 `${{ }}`로 직접 끼워 넣지 말고 반드시 `env:`로 넘겨 환경변수로만 참조하세요. 워크플로우 파일을 더 고칠 때 같은 패턴을 반복하지 않도록 주의.
5. **day7, day9는 아직 확인 안 됨.** day10만 확인했고, day7·9가 이미 게시됐는지 `state.json`을 다시 보세요. day7은 `CATCHUP_DAYS`=3 경계 근처라 너무 늦으면 건너뛸 수 있습니다.
6. 수정 후 반드시 — `content_plan.json` → `python validate.py` / `daily.py` → `python test_pick_post.py` / 워크플로우 YAML → `python -c "import yaml; yaml.safe_load(...)"`로 문법 확인
7. 원격: https://github.com/flag0524/insta_workflow (public) · 워크플로우: `.github/workflows/daily-post.yml`
