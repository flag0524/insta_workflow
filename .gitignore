# API 키. 절대 커밋 금지
# .env.bak, .env.local 등 파생 파일까지 전부 막고 양식 파일만 예외로 둔다
.env*
!.env.example

# 생성물 — 렌더된 PNG와 MP4
out/

# 게시 이력. 실행 환경마다 다름
state.json

# 실행 로그
daily.log

__pycache__/
*.pyc

# BGM 음원 파일 (라이선스 파일을 레포에 넣지 않음)
assets/bgm/*.mp3
