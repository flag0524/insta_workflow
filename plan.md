# 인스타 릴스 자동 게시 30일 계획

**계정 니치**: AI/자동화 실무 팁
**타깃**: 개발자, 1인 사업자, 사이드 프로젝트 운영자 (20~40대)
**기간**: 2026-09-08(화) ~ 2026-10-07(수), 30일 / 1일 1건
**포맷**: 전량 릴스(9:16, 1080x1920)
**스택**: Python 3.11 + Windows 작업 스케줄러 + Playwright + ffmpeg + Instagram Graph API

---

## 0. 먼저 알아야 할 제약 3가지

### (1) 트렌딩 오디오를 쓸 수 없다 — 자동화의 실제 비용

Graph API로 게시하는 릴스는 MP4에 인코딩된 오디오만 재생됩니다. 인스타그램 음원 라이브러리의 인기 오디오는 앱에서 수동 업로드할 때만 선택할 수 있습니다.

트렌딩 오디오는 릴스 도달의 큰 축입니다. 이걸 포기하는 대신 통제 가능한 다른 변수로 보완합니다.

| 포기 | 보완 |
|---|---|
| 트렌딩 오디오 파도 | 로열티 프리 BGM을 ffmpeg로 믹싱 + 훅·자막·루프 최적화 |

**결정 (2026-09-08): 전량 자동으로 간다.** 월 4건을 수동 발행하는 하이브리드를 검토했지만 채택하지 않았습니다.

수동 발행일을 두면 "매일 자동"이라는 이 계정의 전제 자체가 깨집니다. 30일 내내 손대지 않는 것이 day15·day30 콘텐츠의 근거이기도 합니다. 트렌딩 오디오는 포기하고, 대신 훅·자막·루프·발행 시간으로 승부합니다.

30일 실측 후 재검토할 수 있습니다. 지금 결정을 되돌리려면 `content_plan.json`에 `manual: true` 플래그를 추가하고 `daily.py`의 `pick_post`에서 걸러내면 됩니다.

### (2) 조회수 폭발은 보장 대상이 아니다

제가 설계할 수 있는 건 통제 가능한 변수까지입니다.

- 첫 3초 훅 (5가지 유형 로테이션)
- 재생 완료율을 노린 길이 설계 (7~55초 3단 배분)
- 이음매 없는 루프 구조
- 자막 밀도와 가독성
- 발행 시간대

도달·저장·팔로우 전환은 이 변수들의 결과이지 입력이 아닙니다. 30일 후 인사이트 실측으로 조정하는 전제로 설계했습니다.

### (3) 영상은 공개 URL이어야 한다

`POST /{ig-user-id}/media`는 `video_url`에 공개 접근 가능한 URL만 받습니다. 로컬 MP4 직접 업로드는 불가능해서 호스팅 단계가 파이프라인에 반드시 들어갑니다. 이미지와 달리 영상은 용량이 커서 imgbb를 쓸 수 없고, Cloudinary 무료 티어(월 25GB)를 씁니다.

---

## 1. 자동 실행 흐름

```
Windows 작업 스케줄러 (매일 07:15 / 09:45 / 20:15 — 3회 트리거)
  │
  └─> python daily.py
        │
        1. content_plan.json 로드
        2. 게시할 항목 1건 선택
        │     (a) 오늘 날짜 + publish_at 45분 전부터 → 우선
        │     (b) 없으면 지난 미게시 항목을 오래된 순으로 따라잡기 (최대 3일)
        │         단 date_sensitive 항목은 그날이 지나면 건너뜀
        │     둘 다 없으면 즉시 종료 (exit 0)
        │     state.json에 published 기록 있으면 종료 (중복 방지)
        │
        3. [장면 렌더링]  Playwright
        │     scenes[] 각 항목을 templates/scene.html에 주입
        │     1080x1920 PNG 캡처 → out/day{N}/s01.png ...
        │     장면당 1장. 보통 4~7장
        │
        4. [영상 조립]  ffmpeg
        │     각 장면 PNG에 zoompan(켄번스) 적용
        │     장면 간 xfade 0.25초 크로스페이드
        │     BGM 믹싱 + 마지막 0.3초 페이드아웃
        │     loop_type이 seamless면 첫/끝 프레임 밝기를 맞춰 이음매 제거
        │     → out/day{N}/reel.mp4  (H.264 / yuv420p / 30fps / AAC)
        │
        5. [커버 추출]
        │     cover_scene 인덱스의 PNG를 커버로 사용
        │
        6. [호스팅]  Cloudinary 업로드 → 공개 URL 2개(영상, 커버)
        │
        7. [업로드]  Graph API
        │     POST /{ig-user-id}/media
        │        media_type=REELS, video_url, cover_url, caption, share_to_feed=true
        │     → creation_id
        │     GET /{creation_id}?fields=status_code 폴링
        │        IN_PROGRESS면 5초 간격, 최대 5분 대기
        │        FINISHED면 진행 / ERROR면 중단
        │     POST /{ig-user-id}/media_publish (creation_id)
        │
        8. [기록]
        │     state.json에 day, 게시 시각, media_id, 성공/실패 append
        │     실패 시 텔레그램 알림 + 다음 트리거에서 재시도
```

### 왜 프레임 900장 캡처가 아니라 장면 PNG + ffmpeg 모션인가

30fps × 30초 = 900프레임을 Playwright로 캡처하면 영상 1건당 45초 이상 걸리고, CSS 애니메이션 시점을 결정적으로 고정하는 코드가 따로 필요합니다.

정보성 팁 릴스에 실제로 필요한 모션은 장면 전환과 완만한 줌뿐입니다. 이건 ffmpeg의 `zoompan` + `xfade`가 이미 합니다. 장면당 PNG 1장이면 렌더가 8장으로 끝납니다.

**포기한 것**: 타이핑 효과, 숫자 카운터 증가처럼 장면 내부에서 변하는 모션.
**나중에 필요하면**: 훅 구간(0~3초)만 프레임 캡처로 올리면 됩니다. 전체를 바꿀 필요 없습니다.

---

## 2. 사전 준비 (1회, 수동)

| 항목 | 내용 |
|---|---|
| 인스타 계정 | 프로페셔널(비즈니스/크리에이터) 전환 필수 — 현재 `@flag_21` / MEDIA_CREATOR |
| API 계열 | **Instagram API with Instagram Login** (`graph.instagram.com`). 페이스북 페이지 연결이 **불필요**합니다 |
| 페이스북 페이지 | 불필요. (페이스북 로그인 방식을 쓴다면 `graph.facebook.com`으로 바꾸고 페이지 연결이 필요합니다) |
| 권한 | `instagram_business_basic`, `instagram_business_content_publish` — 확인 완료 |
| 액세스 토큰 | 장기 토큰(60일 만료). `python daily.py --check-env`가 실제로 호출해 검증합니다 |
| IG_USER_ID | 사용자명이 아니라 **숫자 ID**입니다 (`28152757404390183`) |
| Cloudinary | 무료 계정 (월 25GB 대역폭) |
| ffmpeg | PATH 등록 필요 |
| python 경로 | 스케줄러에는 **시스템 python 절대 경로**를 등록합니다. PATH의 `python`이 다른 가상환경을 가리킬 수 있고, 그러면 스케줄러 실행만 ImportError로 실패합니다 |
| BGM | 로열티 프리 3~5곡 (`assets/bgm/`) |
| 텔레그램 봇 | 실패 알림용 (선택) |

**릴스 스펙 한도**
- 길이 3초 ~ 15분 / 용량 최대 1GB
- 코덱 H.264 + AAC, `yuv420p` 필수 (없으면 일부 기기에서 재생 실패)
- 9:16 권장, 1080x1920
- 게시 API 호출 24시간당 25건 (하루 1건이라 여유)

---

## 3. 릴스 설계 규칙

### 훅 5유형 로테이션

같은 훅 패턴을 30일 반복하면 피드에서 소비됩니다. 5유형을 돌립니다.

| 유형 | 형태 | 예시 |
|---|---|---|
| H1 손실회피 | "이거 모르면 ○○ 날립니다" | "이거 모르면 매주 6시간 날립니다" |
| H2 숫자 | "○줄로 끝나는 △△" | "20줄로 끝나는 웹 데이터 수집" |
| H3 반전 | "다들 ○○ 하는데 사실 △△" | "다들 프롬프트를 길게 쓰는데, 사실 반대입니다" |
| H4 질문 | "아직도 ○○ 손으로 하세요?" | "아직도 엑셀 취합 손으로 하세요?" |
| H5 결과선공개 | "이거 하나로 ○○ 됐습니다" | "파일 하나로 AI 실수가 70% 줄었습니다" |

훅 자막은 0~3초 구간에 최대 2줄, 한 줄 14자 이내로 고정합니다.

위치는 **화면 중앙**입니다. 처음에는 상단 1/3에 두려 했지만 실제로 렌더해 보니 아래 2/3가 텅 비어 힘이 없었습니다. 하단 320px은 어차피 인스타 UI가 덮으므로 중앙 정렬이 안전 영역 안에서 가장 꽉 차 보입니다.

**연속 반복 금지**: 같은 훅 유형이 이틀 연속 나오지 않게 합니다. 균등 배분(6/6/6/6/6)은 목표가 아닙니다. 목표는 피드에서 같은 패턴이 반복돼 보이지 않는 것이고, 그건 연속만 막으면 충족됩니다. 현재 분포는 H1 6 · H2 6 · H3 7 · H4 6 · H5 5입니다.

이 규칙은 `validate.py`가 검사합니다. 계획을 수정하면 반드시 다시 돌리세요.

### 길이 3단 배분

| 구간 | 건수 | 목적 |
|---|---|---|
| 8~12초 (루프형) | 10건 | 재생 완료율·반복 재생으로 도달 확보 |
| 20~35초 (정보형) | 15건 | 저장 유도. 계정의 주력 |
| 40~55초 (심화형) | 5건 | 팔로우 전환. 주 1건 배치 |

### 루프 구조

`loop_type: "seamless"`인 항목은 마지막 장면이 첫 장면과 이어지게 써서 반복 재생을 유도합니다. 8~12초 루프형 10건 전부에 적용했습니다.

`loop_type: "cta_end"`는 마지막 장면에서 명시적으로 저장·팔로우를 요청합니다. 정보형·심화형에 적용했습니다.

### 자막 규칙

- 모든 장면에 자막. 무음 시청이 기본값입니다
- 한 장면당 최대 2줄, 한 줄 14자
- 안전 영역: 상단 220px, 하단 320px 비워둠 (UI 겹침 방지)

---

## 4. 콘텐츠 전략

### 주차별 테마

| 주차 | 기간 | 테마 |
|---|---|---|
| W1 | 09-08 ~ 09-14 | AI 코딩 에이전트 실무 |
| W2 | 09-15 ~ 09-21 | 반복 업무 자동화 |
| W3 | 09-22 ~ 09-28 | 콘텐츠 자동화 (추석 연휴 포함) |
| W4 | 09-29 ~ 10-05 | 데이터·문서 자동화 + 운영 |
| W5 | 10-06 ~ 10-07 | 30일 회고 + 실측 공개 |

### 필러 비율

교육/하우투 60% (18건) · 사례/실측 20% (6건) · 도구 소개 10% (3건) · 참여 유도 10% (3건)

### 연휴 처리

추석 연휴(9/24 목 ~ 9/27 일)와 개천절(10/3 토)에는 학습 부담이 큰 하우투 대신 8~12초 루프형 가벼운 콘텐츠를 배치하고, 발행 시간도 오전 10:30으로 옮겼습니다.

주말·연휴 슬롯이 정확히 10개이고 루프형 콘텐츠도 10건이라 일대일로 대응합니다. 연휴 구간은 내용까지 날짜에 맞췄습니다.

| day | 날짜 | 콘텐츠 |
|---|---|---|
| 17 | 09-24(목) 연휴 첫날 | 연휴에도 계정이 도는 이유 |
| 18 | 09-25(금) **추석 당일** | 추석 인사와 자동화 근황 |
| 19 | 09-26(토) | 자동화 공감 5선 |
| 20 | 09-27(일) 연휴 마지막 | 연휴 끝, 내일 쓸 체크리스트 (20:00) |
| 26 | 10-03(토) 개천절 | 릴스 업로드 골든타임 |

---

## 5. 발행 시간

| 슬롯 | 시간 | 건수 | 요일 | 근거 |
|---|---|---|---|---|
| 아침 | 08:00 | 8 | 화·목 | 출근길 스크롤 |
| 주말/연휴 | 10:30 | 9 | 토·일·공휴일 | 늦은 기상 후 첫 스크롤 |
| 저녁 | 21:00 | 12 | 월·수·금 | 퇴근 후 코어 타임 |
| 예외 | 20:00 | 1 | day20만 | 연휴 마지막 밤(09-27). 다음 날 출근을 앞둔 시간대를 노림 |

일반 벤치마크 기준이며 이 계정의 실측값이 아닙니다. 30일 후 인사이트의 팔로워 활동 시간대로 재조정합니다.

---

## 6. 캡션 구조

릴스는 캡션이 접혀서 노출되므로 첫 줄 비중이 큽니다.

```
[첫 줄]   영상 훅과 다른 문장. 125자 안에 들어가야 함
[본문]    3~5줄. 영상에서 못 담은 보충 정보
[CTA]     1줄. 저장/댓글/공유 중 하나만
[해시태그] 15~18개. 줄바꿈 여러 번으로 분리
```

## 7. 해시태그

동일 세트 30일 반복은 스팸 신호입니다. 3개 풀을 주제에 맞춰 로테이션합니다.

세트 구성: 대형(10만+) 3 + 중형(1만~10만) 6 + 소형/니치(1만 미만) 7 + 브랜드 1 = 17개

- **세트 A** — AI 코딩/도구
- **세트 B** — 업무 자동화/파이썬
- **세트 C** — 콘텐츠·마케팅/1인 사업

실제 태그는 `content_plan.json`의 `hashtag_sets`에 있습니다.

---

## 8. 파일 구조

```
인스타 자동화/
├── plan.md               # 이 문서
├── checklist.md
├── context-notes.md
├── content_plan.json     # 30일 릴스 데이터
├── validate.py           # 계획 데이터 규칙 검사
├── daily.py              # 단일 엔트리포인트 (구현 완료)
├── templates/
│   └── scene.html        # 장면 1종 (layout 필드로 분기)
├── assets/bgm/           # 로열티 프리 BGM (아직 비어 있음 — 없으면 무음)
├── out/                  # PNG + MP4
├── state.json            # 게시 이력 (첫 게시 때 생성)
└── .env.example          # .env로 복사해 값 입력
```

**실행 방법**

```
python daily.py                  # 오늘자 1건 자동 실행 (스케줄러가 호출)
python daily.py --day 1 --dry-run  # 영상까지만 만들고 업로드 안 함
python daily.py --check-env      # 실행 환경 점검
python validate.py               # 계획 데이터 검사
```

`daily.py` 하나로 둡니다. 렌더·조립·업로드가 순차 1회 실행이고 단계 간 재사용이 없어 분리 이득이 없습니다. 250줄 안쪽 예상.

템플릿도 1개입니다. 표지·요점·CTA를 파일 3개로 나눌 만큼 다르지 않아서 `layout` 필드로 분기합니다.

---

## 9. 실패 시나리오

| 상황 | 대응 |
|---|---|
| 인코딩 status_code = ERROR | 텔레그램 알림. MP4 스펙(yuv420p) 우선 확인 |
| 폴링 5분 초과 | 다음 트리거에서 creation_id 재확인 후 게시 |
| Cloudinary 업로드 실패 | 3회 재시도 → 알림 |
| 토큰 만료(60일) | 55일째 갱신 알림. 30일 계획 내에는 미발생 |
| PC 꺼져 있음 | 작업 스케줄러 "놓친 작업 즉시 실행" 활성화 |
| 중복 게시 | state.json day 기록으로 차단 |
| ffmpeg 미설치 | 시작 시 `ffmpeg -version` 체크 후 즉시 종료 |
| 원인 추적 | 모든 실행이 `daily.log`에 기록됩니다. 텔레그램을 설정하지 않았다면 여기가 유일한 단서입니다 |
| 하루가 통째로 밀림 | 다음 트리거가 **지난 미게시 항목을 따라잡습니다**(최대 `CATCHUP_DAYS`=3일). 오늘 것이 먼저고, 그다음 밀린 것 중 가장 오래된 순 |
| 명절 콘텐츠가 늦게 나감 | `date_sensitive: true`인 항목은 그날이 지나면 게시하지 않고 건너뜁니다 (day17·18·20) |

---

## 10. 성공 기준

1. 30일 중 28일 이상 자동 게시 성공 (93%)
2. 수동 개입 0회로 연속 7일 이상 무중단
3. 실패 시 30분 안에 알림 도달
4. 릴스 평균 재생 완료율 측정값 확보 (목표치는 1주차 실측 후 설정)

조회수·팔로워 수치는 성공 기준에 넣지 않았습니다. 통제 불가능한 값을 기준으로 삼으면 파이프라인이 정상인지 판단할 수 없습니다.

---

## 11. 미확정 사항

- Meta 앱 심사: 본인 계정에만 게시하면 개발 모드로 가능하나 토큰 발급은 직접 진행 필요
- BGM 출처 미정. 유튜브 오디오 라이브러리 또는 Pixabay 무료 음원 예정. **넣지 않아도 무음으로 동작은 합니다**
- 계정 실제 활동 시간대는 30일 데이터 후 확인

해결된 것: 트렌딩 오디오 하이브리드는 미도입으로 확정 (0절 참고).
